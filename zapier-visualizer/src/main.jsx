import React from "react";
import ReactDOM from "react-dom/client";
import ZapierVisualizer from "./ZapierVisualizer.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ZapierVisualizer />
  </React.StrictMode>
);
